# Rocq / Coq build artifacts
*.vo
*.vos
*.vok
*.glob
*.aux
*.cache
.lia.cache
.nia.cache
*.crashcoqide

# Incremental-cache artefacts (CI-only; content manifests + per-file PA chunks).
/.vo-manifest
/.vo-manifest-theories
/.palog/

# Generated by coq_makefile (root only -- nested project Makefiles are kept)
/Makefile
/Makefile.conf
/Makefile.full
/Makefile.full.conf
/Makefile.gen
/Makefile.gen.conf
/Makefile.gen.full
/Makefile.gen.full.conf
*.d

# .NET build artefacts (tools/* console apps, test harnesses)
bin/
obj/
*.user
*.suo

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Prebuilt oracle binary (from CI artifacts for local harden/vector gen; reproducible via Dockerfile+make)
oracle/oracle_bin
oracle/extracted.ml
oracle/circular_cook_extracted.ml
oracle/circular_cook_extracted.mli
oracle/i_circular_probe

# Local agent / MCP scratch (not part of the corpus)
/agent-tools/
/mcps/

# Downloaded CI artifacts (reproducible via Dockerfile+make; never commit)
.ci-artifacts/

# coq_makefile / rocq makefile output from ad-hoc target builds (any suffix).
Makefile.*.conf
Makefile.5*
