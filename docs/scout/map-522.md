# Map — #522 RelateNG honest classifier (bar 1 → bar 2)

A wayfinder map. Tickets already live as GitHub sub-issues of
[#522](https://github.com/grootstebozewolf/NetTopologySuite.Proofs/issues/522)
(`522-a` … `522-l`). This file is the **living frontier**. Charted
2026-08-29; wrap-up refresh 2026-08-30.

> **Tracker.** The children stay on GitHub. This map does not mint claimIds
> and does not replace the epic. Leftovers after wrap-up live in
> [`map-522-leftovers.md`](map-522-leftovers.md). Leftover ids are
> precomposed Roman numerals. **`Ⅰ`** is the T-junction / partial-edge
> kiss. **`Ⅱ`** is obtuse-at-v. **`Ⅲ`** is the exterior-side
> one-sided T. **`Ⅳ`** is the interior-side stem. **`Ⅴ`** is
> mixed-cone. **`Ⅵ`** is same-cone inhabitance. Do **not** mint `522-n`.
> Do not remint ADR-0004. Do not steal closed letters.

## Destination

**Bar level 2 across every wired triangle regime**, after each regime has
climbed through bar 1. A decline stays a decline (`UNSUPPORTED` on the
oracle wire; `im_unsupported` in Coq), never a confident `FFFFFFFFF`.

That destination is **met for the wired regimes**. Classifier fills are
not reminted. Completeness is false. The epic stays open until the owner
signs off on [`522-closing-summary.md`](522-closing-summary.md).

Bar 1 = an emitted regime is true geometry against the specified interior
(`0 < gtri`, ADR-0003). Bar 2 = each of the nine cells is the true DE-9IM
value for that geometry (gtri vocabulary).

## Notes

**Item 1 of the original ask is done.** PR #530 replaced the fallthrough
with `im_unsupported`. Overlapping triangles no longer compute as disjoint.

**Bar 1 is wired for the named regimes.** Overlap #570 / `522-b`, disjoint
#571 / `522-c`, vertex-touch #572 / `522-i`, contains via the #567
predicates plus the `contains_b` bridge. Completeness is **false**: the
T-junction and obtuse-at-v pairs still emit `TPR_Unsupported` (#577 /
`522-j`, leftover `522-m`). Five names are not a partition. Do not invent
those certificates unless asked. T-junction is leftover `Ⅰ`.
Obtuse-at-v is leftover `Ⅱ`. Do not swap them.

**Bar 2 gtri cells landed, not a remint.** Disjoint FF2FF1212 (#587),
contains 212FF1FF2 (#592), touch-edge FF2F11212 (#593), overlap 212101212
(#594). The classifier still emits the shared pins. Reminting those pins
is later, **not** #575 / `522-f`.

**Oracle token is on `main`.** #588 Coq + #595 harness. Honest decline
vector is the T-junction. The #530 / #571 pair is classified disjoint.

**Frozen anchors.** `touch_int_ext_exclusion`,
`touch_triangle_ii_separation_not_unconditional`,
`triangles_touch_on_shared_edge`. Ray parity enters only via ADR-0003.

**Method.** Read → Green (Qed, documented failure, or counterexample) →
Refactor. Maintainable > reliable > fast. Cite theorems by name.

## Ticket table (live, 2026-08-30)

| Ticket | claimId | Horizon | Blockers | State | Takeable? |
|---|---|---|---|---|---|
| #567 Regime predicates | `522-a` | Immediate | — | DoD met (#579 / #586); leftover carved this letter | — |
| #568 Ring-inclusion groundwork | `522-g` | Expectant | — | done via #585 | — |
| #569 Carve Scholar Sam's facts | `522-k` | Immediate | — | done via #590 | — |
| #570 Overlap at bar 1 | `522-b` | Immediate | #567 | done via #580 | — |
| #571 Disjoint at bar 1 | `522-c` | Immediate | #567 | done via #581 | — |
| #572 Vertex-touch at bar 1 | `522-i` | Non-urgent | #567 | done via #582 | — |
| #573 Bar 2 beachhead | `522-d` | Expectant | #571 #568 | done via #587 | — |
| #574 Prepared evaluate consults cache | `522-e` | Expectant | #571 | done via #591 | — |
| #575 Oracle UNSUPPORTED token | `522-f` | Expectant | #570 #571 | done via #588 + #595 | — |
| #576 Bar 2 contains / touch / overlap | `522-h` | Expectant | #573 #568 | DoD met (#592 / #593 / #594); paperwork this letter | — |
| #577 Classifier completeness | `522-j` | Non-urgent | #570 #571 #572 | done via #583; finding: completeness is false | — |
| #578 Wrap-up | `522-l` | Expectant | #574 #575 #576 #577 #569 | **this letter** | — |

Unofficial leftover (no child issue): `522-m` filtered CCW-completeness,
merged as #584. Still false (obtuse-at-v).

## Decisions so far

- Item 1 / honesty sentinel — #530.
- `522-a` predicates + exclusivity — #579; `contains_b` → closed containment
  — #586. Vacuity witnesses flipped. Named leftover on #567:
  `TPR_TouchEdge` exclusivity vs the four gtri predicates.
- Bar 1 overlap / disjoint / vertex-touch — #580 #581 #582.
- Completeness false (T-junction) — #583 / `522-j`. Retry still false
  (obtuse-at-v) — #584 / `522-m`.
- Ring-inclusion disks / dim-1 — #585 / `522-g`.
- OGC gtri cells — #587 / #592 / #593 / #594. Classifier fills stay the
  shared pins.
- Wire token `UNSUPPORTED` — #588 + harness #595 / `522-f`.
- Scholar Sam carve — #590 / `522-k`.
- Cache-consulting evaluate — #591 / `522-e`.
- Wrap-up — #596 / `522-l`.
- #567 leftover carve — #597 / `522-a-touch-edge-carve`.

## Fog

- **#567 DoD met; carve on `main` via #597.** TouchEdge exclusivity is named,
  not proved. See [`map-522-leftovers.md`](map-522-leftovers.md).
- **Disjoint fill remint** (FFFFFFFFF → FF2FF1212) is unnamed. Not `522-f`.
  See [`map-522-leftovers.md`](map-522-leftovers.md). Ask before minting.
- **Mutual vertex-in-open-edge sliver** is leftover `Ⅰ` (bar 1 landed:
  `RelateNGTouchPartialEdge.v : triangle_pair_regime_touchpartial`).
  **Obtuse-at-v** is leftover `Ⅱ`
  (`RelateNGTouchObtuse.v : triangle_pair_regime_obtuse`).
  Leftover `Ⅲ` is the exterior-side one-sided T (`Ⅲ∨Ⅳ` xor, two
  witnesses). Leftover `Ⅳ` is the interior-side stem
  (`RelateNGComplete.v : interior_side_pair_inhabits`).
  Leftover `Ⅴ` is mixed-cone
  (`RelateNGTouchMixedCone.v : triangle_pair_regime_mixedcone`).
  Leftover `Ⅵ` is same-cone inhabitance
  (`RelateNGTouchSameCone.v : triangle_pair_regime_samecone`).
  Completeness is an unnamed lens
  (`RelateNGComplete.v : triangle_pair_regime_ccw_incomplete`).
  Epic #522 stop is QED ∨ QEX
  (`RelateNGEpic522.v : ticket_522_qed_or_qex`). Leftover `Ⅰ`–`Ⅵ`
  are classified (`RelateNGEpic522.v : ticket_522_classified_qed_or_qex`).

- **Full RelateNG noding** and the line×line **Touches-vs-Share fill split**
  are #67 leftovers. Living list: [`docs/relate-ng-status.md`](../relate-ng-status.md).
- **Nine-cell `geom_de9im_pointset`** still open (ADR-0003 half-open leftover).
- **Empty/empty relate** stays parked (epic out-of-scope).
- **Ticket 523** (`F` vs not-computed on `CURVE_RELATE_MATRIX`) is a sibling.
  Grill: [`map-523.md`](map-523.md). Spec: [`spec-523.md`](spec-523.md).
  Tickets: [`15-523-a`](tickets/closed/15-523-a-eb-refuse.md) [#603](https://github.com/grootstebozewolf/NetTopologySuite.Proofs/issues/603),
  [`16-523-b`](tickets/closed/16-523-b-cell-unknown.md) [#604](https://github.com/grootstebozewolf/NetTopologySuite.Proofs/issues/604),
  [`17-523-c`](tickets/closed/17-523-c-driver-alphabet.md) [#605](https://github.com/grootstebozewolf/NetTopologySuite.Proofs/issues/605).
  Children `523-a` / `523-b` / `523-c` landed. Still open, not accepted.
  Do not start another wayfinder. Owner resolve-or-accept is the gate.

## Frontier

**#578 / `522-l`** landed via #596. Leftovers after wrap-up:
[`map-522-leftovers.md`](map-522-leftovers.md). Epic #522 stays open for
owner sign-off. #567 carve is on `main` via #597. Leftover `Ⅰ` is the
mutual vertex-in-open-edge sliver. Leftover `Ⅱ` is obtuse-at-v
(`RelateNGTouchObtuse.v : triangle_pair_regime_obtuse`).
Leftover `Ⅲ` is the exterior-side one-sided T (`Ⅲ∨Ⅳ` xor, two
witnesses). Leftover `Ⅳ` is the interior-side stem
(`RelateNGComplete.v : interior_side_pair_inhabits`).
Leftover `Ⅴ` is mixed-cone
(`RelateNGTouchMixedCone.v : triangle_pair_regime_mixedcone`).
Leftover `Ⅵ` is same-cone inhabitance
(`RelateNGTouchSameCone.v : triangle_pair_regime_samecone`).
Completeness is an unnamed lens. Leftover `Ⅶ` is already #642.

```
#530 honesty ════════════════════════════════════ done

#567 522-a ── predicates + bridge ── closed; TouchEdge exclusivity carved
#568 522-g ── disks / dim-1 ────────────────────────────── closed
#569 522-k ── triage-doc carve ────────────────────────── closed
#570 522-b ── overlap bar 1 ────────────────────────────── closed
#571 522-c ── disjoint bar 1 ──┬── #574 522-e cache ──── closed via #591
                               └── #575 522-f token ──── closed via #588+#595
#572 522-i ── vertex-touch bar 1 ───────────────────────── closed
#573 522-d ── FF2FF1212 cells ── #576 522-h ── #592 / #593 / #594
#577 522-j ── completeness false ── 522-m still false ── closed (finding)
#578 522-l ── wrap-up ── closed via #596; epic waits on owner sign-off
```
