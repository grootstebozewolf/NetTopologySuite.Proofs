# Deferred-proof registry for Admitted theorems.
#
# This is the THIRD tier of the corpus's Admitted-classification system,
# complementing `docs/admitted-counterexamples.txt`:
#
#   Tier 1: standard Admitted -- BUILD FAILURE.  No registry entry.
#   Tier 2: Admitted with verified counterexample -> registered in
#           `docs/admitted-counterexamples.txt`.  Means "the theorem
#           as currently stated CANNOT be proved because the statement
#           is too strong; the counterexample proves a weaker version
#           is the most that's achievable."
#   Tier 3: Admitted with documented proof structure -> registered HERE.
#           Means "the theorem IS provable; the proof structure is
#           documented; the work is multi-session and hasn't been
#           completed yet."
#
# The CI script `scripts/check_admitted.sh` enforces this: every
# `Admitted.` in the corpus must appear in EXACTLY ONE of the two
# registries (counterexample or deferred-proof) -- or build fails.
#
# Format (one entry per non-comment, non-blank line):
#
#   file:theorem_name | proof_structure_doc | section_references
#
# - file: path relative to repo root.
# - theorem_name: the exact Coq identifier of the Theorem/Lemma.
# - proof_structure_doc: a doc with the proof structure / approach
#   sketched for the follow-up session.
# - section_references: anchors within the doc.
#
# SEMANTICS DIFFERENCE FROM COUNTEREXAMPLE TIER:
#
# A counterexample-registry entry says "the theorem is FALSE as
# stated; here's the verified witness."  These entries are permanent
# (or change only when the theorem is re-stated).
#
# A deferred-proof entry says "the theorem is TRUE as stated; here's
# the proof structure that needs to be formalised."  These entries
# are TEMPORARY -- they come off the registry when the proof lands.
#
# Removing a deferred-proof entry without proving the theorem is a
# violation of the corpus's epistemic invariant.
#
# Adding an entry here requires:
#   1. The Coq theorem statement in a .v file (with `Admitted.`).
#   2. A proof_structure_doc with enough detail that a future
#      contributor can resume the work without re-deriving the design.
#   3. Section references that point at the specific structure
#      (algorithm, invariants, magnitude bookkeeping, etc.).
#
# An entry here is NOT a license to skip Coq work indefinitely.  It's
# an explicit IOU: the corpus tracks the gap, the policy framework
# enforces removal-via-proof, and CI catches drift.

# ============================================================================
# Slice A Piece 5b: Shewchuk Theorem 13 formalisation.
# ============================================================================

# `fast_expansion_sum_nonoverlap_shewchuk` -- general unconditional
# headline for Shewchuk Theorem 13 (1997, ~1 page of dense magnitude
# analysis).  THESIS-SCALE.  Re-classified 2026-05-29 after 17 sessions
# of cumulative work established the structural floor; previous estimate
# of "3-4 sessions" did not survive contact with the Route 1 collapse
# artifacts.  Reclassification rationale below.
#
# WHAT IS QED-CLOSED in
# `theories-flocq/B64_FastExpansionSum_Shewchuk_Route2.v`:
#   - `fast_expansion_sum_nonoverlap_shewchuk_general_conditional`:
#     under `cascade_pathA_chain` on the initial state, the headline
#     follows.  This is the load-bearing composition step; the
#     "Work is composition, not new mathematics" path lands here.
#   - `fast_expansion_sum_nonoverlap_shewchuk_two_singletons`:
#     UNCONDITIONAL length-2 headline.
#   - `fast_expansion_sum_nonoverlap_shewchuk_int_safe_singletons`:
#     UNCONDITIONAL length-2 int-safe headline.
#   - `fast_expansion_sum_nonoverlap_shewchuk_int_safe_two_pairs`:
#     UNCONDITIONAL (2,2) int-safe headline.  Covers all practical
#     orient2d-shaped Stage D inputs under `coord_int_safe`.
#
# Plus the supporting machinery (200+ lemmas): cascade_invariant,
# cascade_run, cascade_h_chain under Path A (pos + neg),
# run_bound_step_preserves, b64_plus_int_exact + integer cascade,
# compress structural lemmas, sort-by-abs Forall preservation, etc.
#
# WHAT REMAINS OPEN -- the general unconditional headline only:
#   Deriving `cascade_pathA_chain (initial_cascade_state x prov) rest`
#   from the actual preconditions of the theorem, namely
#     `fast_expansion_sum_safe e f /\
#      nonoverlap_shewchuk e /\
#      nonoverlap_shewchuk f`
#   for arbitrary expansions `e f` (i.e. closing
#   `_general_conditional` by discharging its `cascade_pathA_chain`
#   hypothesis from these three assumptions rather than positing it).
#   This is precisely Shewchuk Theorem 13's deep magnitude bookkeeping:
#   per-element provenance tracking + cross-prov mixed-sign
#   similar-magnitude case analysis + round-to-even boundary handling.
#   Session 3 quantified the gap as "roughly 2^53 too loose" between
#   the cascade invariant's natural bound and the h-chain link
#   required for arbitrary inputs.
#
# Session history (collapse artifacts in `docs/`):
#   - Route 3 (descending strict_succ chain on input): ruled out
#     2026-05-24 by `e=[4.0], f=[3.0]` counterexample.
#   - Route 2 Session 2: clause (c) collapse on Form A state.
#   - Route 1 design session: third design recommended.
#   - Route 1 Sessions 2-12: cascade_state augmentation (Form B with
#     cs_prov + per-source max), refined clauses, h-chain split as
#     a separate cascade-step lemma; established the 2^53 gap
#     structurally and Qed-closed the conditional headline.
#   - Route 1 Sessions 13-16: integer-safe specialised headlines
#     (the three UNCONDITIONAL Qed-closures listed above).
#   - Route 1 Session 17 (`route1_attempt`, Aborted at length 3+):
#     concrete demonstration of the deferred-proof obstacle inside
#     the corpus.
#
# PRACTICAL IMPACT.  For NetTopologySuite consumers, `_int_safe_two_pairs`
# covers all orient2d Stage D inputs under coord_int_safe (the integer
# geometry contract).  The general unconditional headline is a
# theoretical completion (thesis-scale), not a practical blocker for
# Phase 3 or downstream geometry correctness.
#
# Estimate: thesis-scale (multiple months of focused magnitude
# bookkeeping, comparable in shape to `hobby_lemma_4_3_no_proper`).
# Not attempted in further single-session work without a published
# magnitude-bookkeeping framework (Shewchuk §4-style provenance
# tracking) to lean on.
theories-flocq/B64_FastExpansionSum_Shewchuk.v:fast_expansion_sum_nonoverlap_shewchuk | docs/shewchuk-theorem-13-proof-structure.md | §1,§2.1,§4,§6,§6.1,§6.2,§6.3,§6.4,§6.5

# ============================================================================
# Phase 2 Hobby Theorem 4.1 supporting lemmas.
# ============================================================================

# `hobby_lemma_4_2` -- CLOSED (Qed) in the predicate-fix + proof-attempt
# session 2 sequence (May 2026).  Predicate corrected to strip-shaped
# Minkowski sum per Hobby p.210 (see docs/hobby-lemma-4-2-session-1-
# outcome.md "Design session outcome -- predicate fixed").  Proof
# completed against the corrected predicate via product-sign case split
# (`(px P1 - px P0) * (py P1 - py P0) >= 0` vs `< 0`), each branch
# closing through IZR injectivity + R^- strip bounds + Rtotal_order on
# `tp` vs `tq`.  Per-theorem `Print Assumptions`: only the two
# README-allowlisted classical-reals axioms (no `Classical_Prop.classic`
# pull, despite the file's overall audit-exceptions footprint -- the
# proof avoids Flocq-bridge content).
#
# Registry entry retained only as historical context; no longer
# Admitted.  Remove on next registry compaction.

# `hobby_lemma_4_3_no_proper` -- the genuine thesis-shaped piece of
# Hobby's Lemma 4.3.  Snap-rounding preserves "no proper intersection"
# for any pair of properly-disjoint segments.  Proof sketch:
# docs/hobby-theorem-proof-structure.md §4.
#
# Architectural refinement (May 2026): the original `hobby_lemma_4_3`
# Admitted entry has been refactored into two sub-lemmas:
#   - `hobby_lemma_4_3_shared_endpoint` -- Qed-closed, trivial
#       (snap_round determinism preserves literal endpoint equality).
#   - `hobby_lemma_4_3_no_proper` -- THIS ENTRY -- the load-bearing piece.
# The original `hobby_lemma_4_3` is now Qed-closed by composing the
# two sub-lemmas via case split on `segments_intersect_only_at_endpoints`'s
# disjunctive hypothesis.  `hobby_theorem_4_1_conditional`'s premise is
# unchanged; the deferred-proof obligation is now the narrower
# `no_proper` half.
#
# Approach: use Lemma 4.2 to obtain a (xi, eta) coordinate rotation;
# express D_T(s1) and D_T(s2) as piecewise-linear functions F_1, F_2 in
# (xi, eta); use the tolerance-square bound
# |F_j(xi) - beta_j - gamma_j * xi| < 1/2, combined with the original
# strict ordering, to close the no-proper-intersection ordering at
# snap-rounded endpoints.  Key subtlety: R^- uses the half-open
# convention OPPOSITE to R (Hobby p.210-211).
#
# Tools: piecewise-linear function representation, linear change of
# coordinates, Rle composition.  Estimated scope: 4-6 weeks.
# Thesis-shaped.
#
# Consumer: hobby_lemma_4_3 (now Qed-closed via composition), thence
# hobby_theorem_4_1_conditional; discharging this lemma closes Hobby
# Theorem 4.1 unconditionally.
theories-flocq/HobbyTheorem_b64.v:hobby_lemma_4_3_no_proper | docs/hobby-theorem-proof-structure.md | §4,§7

# ============================================================================
# Phase 3 Milestone 5 deferred entries.
# ============================================================================

# `extract_rings_valid` -- the second thesis-shaped piece of Phase 3
# overlay correctness.  Shows that ring assembly from the labelled
# topology graph produces polygons satisfying all four OGC §6
# conditions (ring_closed, ring_simple, ring_has_minimum_points,
# hole_inside_outer).
#
# The current `extract` (theories/OverlayGraph.v) is the naive
# S2 version: filters edges by `edge_in_result op` and packages
# survivors into a single polygon whose outer_ring is the
# concatenation of all filtered edges' endpoints.  This naive
# form does NOT satisfy `valid_polygon` in general.
#
# The path to a usable proof requires either:
#   - DCEL adoption (S11-S12 in the 16-session plan): doubly-
#     connected edge list with per-vertex incidence + twin/next
#     pointers, then face-traversal-based ring extraction.
#     Estimated 3-4 sessions for the DCEL refactor + 2-3 sessions
#     for the per-condition proofs against the new structure.
#   - A weaker formulation that admits a less-meaningful extract
#     output but is provable.  Loses the headline theorem's
#     semantic strength.
#
# Both paths exceed the per-session budget.  Following S8's pattern:
# register the gap here; carry it as a named hypothesis of
# `overlay_ng_correct_conditional` (S15) when that's stated.
#
# Tools: list-of-edges manipulation, NoDup-on-vertices, OGC §6
# conditions composition.  Pure structural reasoning; no JCT or
# point-set semantics needed at this level.  Estimated 5-7
# sessions of focused work (DCEL path).
#
# Consumer: overlay_ng_correct_conditional (S15) and the Phase 3
# headline correctness theorem.
theories-flocq/OverlayBridge.v:extract_rings_valid | docs/audit-phase3-milestone5.md | §4.3,§7
