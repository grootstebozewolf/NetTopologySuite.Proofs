# Deferred-proof registry for Admitted theorems.
#
# This is the THIRD tier of the corpus's Admitted-classification system,
# complementing `docs/admitted-counterexamples.txt`:
#
#   Tier 1: standard Admitted -- BUILD FAILURE.  No registry entry.
#   Tier 2: Admitted with verified counterexample -> registered in
#           `docs/admitted-counterexamples.txt`.  Means "the theorem
#           as currently stated CANNOT be proved because the statement
#           is too strong; the counterexample proves a weaker version
#           is the most that's achievable."
#   Tier 3: Admitted with documented proof structure -> registered HERE.
#           Means "the theorem IS provable; the proof structure is
#           documented; the work is multi-session and hasn't been
#           completed yet."
#
# The CI script `scripts/check_admitted.sh` enforces this: every
# `Admitted.` in the corpus must appear in EXACTLY ONE of the two
# registries (counterexample or deferred-proof) -- or build fails.
#
# Format (one entry per non-comment, non-blank line):
#
#   file:theorem_name | proof_structure_doc | section_references
#
# - file: path relative to repo root.
# - theorem_name: the exact Coq identifier of the Theorem/Lemma.
# - proof_structure_doc: a doc with the proof structure / approach
#   sketched for the follow-up session.
# - section_references: anchors within the doc.
#
# SEMANTICS DIFFERENCE FROM COUNTEREXAMPLE TIER:
#
# A counterexample-registry entry says "the theorem is FALSE as
# stated; here's the verified witness."  These entries are permanent
# (or change only when the theorem is re-stated).
#
# A deferred-proof entry says "the theorem is TRUE as stated; here's
# the proof structure that needs to be formalised."  These entries
# are TEMPORARY -- they come off the registry when the proof lands.
#
# Removing a deferred-proof entry without proving the theorem is a
# violation of the corpus's epistemic invariant.
#
# Adding an entry here requires:
#   1. The Coq theorem statement in a .v file (with `Admitted.`).
#   2. A proof_structure_doc with enough detail that a future
#      contributor can resume the work without re-deriving the design.
#   3. Section references that point at the specific structure
#      (algorithm, invariants, magnitude bookkeeping, etc.).
#
# An entry here is NOT a license to skip Coq work indefinitely.  It's
# an explicit IOU: the corpus tracks the gap, the policy framework
# enforces removal-via-proof, and CI catches drift.

# ============================================================================
# LIVE ENTRIES
# ============================================================================

# (none -- the deferred-proof registry was EMPTY as of 2026-06-14.
# New live entries added for ArcPointDistance.v D-PT analytical stubs.)

theories/ArcPointDistance.v:point_to_arc_dist_fallback_ends_lower | theories/ArcPointDistance.v (see §4) | fallback to nearer endpoint when radial foot lies outside the directed sweep (arc_span_contains); requires distance monotonicity along arc away from projection

# DISCHARGED 2026-06-21: theories/ArcPointDistance.v:point_to_arc_dist_radial_lower
#   Qed via inCircle_R_zero_implies_equidistant (ArcArcCircles §1c) + point_circle_dist_lower.
#   on_arc X ==> inCircle_R = 0 ==> dist_sq O X = dist_sq O (arc_start) ==> dist O X = r.

# DISCHARGED 2026-06-21: theories/ArcPointDistance.v:point_to_arc_dist_centre_is_r
#   Qed via dist_eq_zero_iff (P=O coord-wise) + inCircle_R_zero_implies_equidistant.
#   on_arc X ==> dist O X = r; P=O ==> dist P X = dist O X = r.
#
# Only `fallback_ends_lower` remains a live deferred-proof stub among the
# original D-PT analytical entries.
# Clamp lemmas for AA/AL landed as thin wrappers over the external cores;
# full monotonicity for the fallback case remains the active item.
#  DISTANCE family.)
#
# DISCHARGED 2026-06-14: theories/EdgeFaceBridge.v:H_bridge_core.  The
# combinatorial core -- the same-face FACE SPLIT
# num_faces (E_minus E d) = num_faces E + 1 -- was PROVED via the generic
# cycle-count splice (PermCycleSplice.cycle_count_surgery) and its instantiation
# NumFacesSplice.num_faces_E_minus_splice.  The bridge fact was then refactored
# from an `Admitted` theorem into a NAMED premise
# `EdgeFaceBridge.H_bridge_premise`, threaded through the same_face / cut-edge
# chain (same_face_twin_disconnect, same_face_twin_is_cut,
# edge_2_connected_twins_sep, H_bridge_well_noded) and DISCHARGED from the planar
# Euler identity in theories/HBridgeEuler.v (H_bridge_premise_from_euler).  The
# headline theories-flocq/OverlayBridge.v:extract_rings_valid now carries the
# planar Euler hypotheses (euler_characteristic over E and its single-edge
# deletions, vertex-count invariance, NoDup E) as explicit named premises -- a
# conditional Qed, not an Admitted.  So there is NO live deferred proof, and the
# whole H_bridge stack lists only allowlist axioms on `Print Assumptions`.

# ============================================================================
# ARCHIVED ENTRIES (closed / moved / superseded -- NOT live)
# ============================================================================
#
# The following entries are no longer live deferred proofs.  Their full
# historical narrative (session histories, superseded rationale, refutation
# witnesses) is preserved in:
#
#   docs/history/deferred-proofs-registry-archive.md
#
# Summary of dispositions:
#
#   - `fast_expansion_sum_nonoverlap_shewchuk` (Shewchuk Thm 13)
#       -> FALSE as stated; MOVED to docs/admitted-counterexamples.txt
#          (2026-06-08).  Counterexample:
#          theories-flocq/B64_Shewchuk_Thm13_counterexample.v;
#          docs/shewchuk-thm13-headline-counterexample.md.
#   - `cascade_pathAB_chain_from_nonoverlap`
#       -> Indischargeable; MOVED to docs/admitted-counterexamples.txt.
#   - `hobby_lemma_4_3_no_proper`
#       -> FALSE as stated; MOVED to docs/admitted-counterexamples.txt.
#          Refutation: theories-flocq/HobbyCounterexample_b64.v;
#          docs/hobby-lemma-4-3-no-proper-refutation.md.
#   - `hobby_lemma_4_2`
#       -> CLOSED (Qed).  No longer Admitted.  Two README-allowlisted
#          classical-reals axioms only.
#   - `extract_rings_valid`
#       -> CLOSED conditional Qed (no registry Admitted).  The combinatorial
#          bridge (edge_2_connected -> twins_in_different_faces) is discharged
#          via EdgeFaceBridge.H_bridge_well_noded; its reach core, once carried
#          as registry Admitted reach lemmas, is now the named premise
#          H_bridge_premise, PROVED from the planar Euler identity in
#          theories/HBridgeEuler.v (H_bridge_premise_from_euler).  The headline
#          carries the planar Euler hypotheses as named premises.
#          [2026-06-14]  See theories-flocq/OverlayBridge.v §8,
#          EdgeConnectivity.v §5, docs/extract-faces-bridge.md §22.

# S15l triangle touch capstone (RelateNG + RelateMatrixTriangle)
# DISCHARGED 2026-06-21: theories/RelateNG.v:touch_triangle_pair_strict_ii_no_common
#   Qed via g_sum + gtri_pos_iff + opposite_sides sign argument.
#   After injection+subst on shares_edge, nra closes all 18 cases using
#   gdbl_A > 0 (from gsA+gsB+gsC=gdbl_A, all gs>0) and gdbl_B > 0 similarly,
#   but opposite_sides forces gdbl_A*gdbl_B < 0. Contradiction.
# DISCHARGED 2026-06-21: theories/RelateNG.v:touch_triangle_pair_bb_cell
#   Qed via tri_bnd_v1/v2/v3 helpers (geom_boundary for each triangle vertex)
#   + all: first [...] tactic that tries each A-vertex as witness, rewrites via Hp1,
#   then applies the matching tri_bnd_v* for B. 18 cases all close.
# DISCHARGED 2026-06-21: theories/RelateNG.v:touch_triangles_satisfy_pointset
#   Qed: real conclusion (not True) assembled from strict_ii_no_common + bb_cell + ee_cell.
# DISCHARGED 2026-06-21: theories/RelateNG.v:touch_triangles_satisfy_pointset_and_general
#   Qed: regime guard (r = TPR_TouchEdge) + touch_triangles_satisfy_pointset.

# DISCHARGED 2026-06-21 (conditional Qed): theories/RelateNG.v:touch_triangle_pair_ii_cell
#   Qed under named H_ii_disjoint : ~exists p, point_set A p /\ point_set B p (H_bridge pattern).
#   Provides cell_ok None SInt SInt. The premise is the point_set form of II separation;
#   algebraic 0<gtri separation is unconditional via touch_triangle_pair_strict_ii_no_common.
#   Full unconditional ii_cell (and geom_de9im_pointset use) deferred on the lift below
#   (point_set_characterises_geometric_interior). See also touch_int_ext_exclusion.

# DISCHARGED 2026-06-21: theories/RelateNG.v:touch_triangle_f_cells_trimmed
#   Qed by splitting/composition: first conjunct via touch_triangle_pair_strict_ii_no_common (Qed);
#   second (the false exclusion) extracted and moved to counterexamples.
#   touch_triangle_f_cells_trimmed itself is now a clean Qed.

# DISCHARGED 2026-06-21: theories/RelateNG.v:relate_triangle_touch
#   Qed (conditional) under added premise `r = TPR_TouchEdge`.
#   Proof uses dispatch + rewrite + reflexivity (H_bridge pattern).

# DISCHARGED 2026-06-28: theories/RelateNG.v:touch_int_ext_exclusion
#   Qed via the in-house algebraic route (NO JCT planar-covering machinery, NO
#   external library). gtri B p < 0 is obtained by refuting "all three B-slacks
#   >= 0" (helper rmin3_neg_intro: 0 <= Rmin(Rmin a b) c forces a,b,c >= 0): in
#   each of the 18 shares x orientation cases, p strictly interior to A
#   (gtri_pos_iff giving the three A-slacks > 0) + opposite_sides + g_sum close by
#   nra. In some cases B's shared-edge slack equals a positive A-slack, so no
#   single B-slack is provably negative -- only the joint all-nonnegative
#   impossibility is, which is exactly what rmin3_neg_intro needs. 3-axiom
#   (classical-reals trio only; confirmed via Print Assumptions).
# NOTE: theories/RelateNG.v:relate_triangle_touch was already DISCHARGED 2026-06-21
#   (Qed at RelateNG.v; see the entry above); its stale live registry line is removed.

# Jordan soundness seam (architectural): in_stratum SInt = point_set (half-open parity via point_in_ring)
# vs true geometric interior {p | 0 < gtri p}. cell_ok None SInt SInt claims II=None under point_set,
# but connecting to geometric_interior_cont (for full geom_de9im_pointset + dim2) requires
# point_set A p <-> geometric_interior_cont A p (non-bnd p) via general_triangle_parity_characterises_interior.
# Long-term refactor; not discharged in this session.
theories/RelateNG.v:point_set_characterises_geometric_interior | JCT / point_set → 0 < gtri lift (for touch_triangle_pair_ii_cell + geom_de9im_pointset) | uses general_triangle_parity_characterises_interior (guarded); see touch_triangle_pair_ii_cell comment in RelateNG.v and GeneralTriangleJCT.v; needed to remove named H_ii_disjoint premise and align stratum SInt with algebraic interiors for triangle touch cells.

# Note: only the triangle touch family from the S15l lift. Rect work on this branch is already Qed or properly trimmed.

# #64 Option-A arc length (atan2 sweep)
# (discharged 2026-06-21) theories/ArcChordLength.v:arc_chord_le_arc_length
#   Qed via arc_chord_dist_sq_via_sweep + chord_subtended_sq (sq identity)
#   + sqrt_Rsqr + chord_le_arc_length (scalar). |sweep| + sin_ge_0 under
#   principal range. See ArcChordLength.v and issue-64-arc... triage.
# (discharged 2026-06-21, Qed) theories/RelateArcAnalytic.v:arc_length_of_nonneg
#   scalar r * abs(theta) nonneg, trivial from Rmult_le_pos + sqrt_pos + Rabs_pos.
#   - `H_bridge_core` (the combinatorial planar same-face => bridge seam)
#       -> DISCHARGED 2026-06-14 via the planar Euler route (see the LIVE-section
#          note above).  Refactored into the named premise H_bridge_premise and
#          proved in theories/HBridgeEuler.v.  No registry Admitted.
#
# Do NOT re-add the moved entries here; they live in the counterexample
# registry by design.
