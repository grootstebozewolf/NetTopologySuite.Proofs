# Audit exceptions for `scripts/audit_axioms.sh`
#
# Each non-comment, non-blank line names a file (relative to repo root)
# whose `Print Assumptions` blocks are exempted from the allowlist check.
# Listed files may pull axioms beyond `docs/axiom-allowlist.txt`; their
# contamination is the *known, transitional* state captured below.
#
# The discipline this file enforces:
#   - Files NOT listed here MUST satisfy the allowlist on every CI run.
#   - Files listed here are tracked for cleanup; removing a file from this
#     list is the final step of refactoring it to be classic-clean.
#   - Adding a file to this list requires PR justification (a comment
#     immediately above the file line explaining what makes the file
#     Category C / transitional and the planned resolution).
#
# Current state: 24 files in `theories-flocq/` are listed below (initial ~11
# for binary ops Bplus/Bminus/Bmult pulling `Classical_Prop.classic` via Flocq,
# plus additional Category C for snap-rounding, overlay, expansion-arithmetic,
# and Stage D as documented per-file).  This contamination is a pre-existing
# condition the corpus inherited from Flocq's binary model; the parametric
# refactor clears files one by one where possible.  Removal from this list is
# the per-file completion criterion for the binary-op lineage.

# Transitional: clean of `classic` once parametric-architecture refactor
# applied per the policy designed in `docs/category-c-policy.md`
# (to be written).
theories-flocq/Orient_b64_exact.v
theories-flocq/Intersect_b64.v
theories-flocq/B64_bridge.v
theories-flocq/Intersect_b64_exact.v
theories-flocq/B64_lib.v
theories-flocq/Orient_b64_R.v
theories-flocq/Validate_binary64_bridge.v
theories-flocq/Orientation_b64.v
theories-flocq/HotPixel_b64.v
theories-flocq/B64_Pff_bridge.v
theories-flocq/Orient_b64_sound.v

# Category C: Shewchuk Theorem 13 pathB exact-cancellation brick.
# b64_TwoSum_exact_of_format_sum / b64_TwoSum_sterbenz_exact / b64_round_0 are
# stated over `b64_TwoSum` / `b64_plus` / `b64_minus` (Flocq Bplus/Bminus) and
# `Binary.B2R`, whose closure carries `Classical_Prop.classic` -- the SAME
# b64-arithmetic / Pff lineage that exempts B64_Pff_bridge.v above (where
# b64_TwoSum lives).  No Admitteds; both lemmas + helper are Qed-closed, and the
# file does NOT use the deferred fast_expansion_sum_nonoverlap_shewchuk (it is a
# building block toward it).  Removable once the parametric refactor reaches the
# binary64 primitives.
theories-flocq/B64_TwoSum_sterbenz.v

# Category C: Shewchuk Theorem 13 obligation O1' (residue granularity).
# residue_ge_half_ulp and its helpers are stated over `generic_format` / `cexp`
# / `mag` / `bpow` and `Binary.B2R`; the binary64 instantiation pulls
# `Classical_Prop.classic` transitively through Flocq's real-number / mag
# machinery -- the SAME b64-arithmetic lineage that exempts B64_TwoSum_sterbenz.v
# and B64_Pff_bridge.v above.  No Admitteds; all lemmas Qed-closed; no dependence
# on the deferred fast_expansion_sum_nonoverlap_shewchuk.  Removable once the
# parametric refactor reaches the binary64 primitives.
theories-flocq/B64_residue_granularity.v

# Category C: Shewchuk Theorem 13 obligation O1 core (nonoverlap head lemmas).
# nonoverlap_shewchuk_cons_zero / nonoverlap_shewchuk_head_replace /
# compress_cons_zero are pure list lemmas over `compress` / `nonoverlap_strict`
# / `Binary.B2R`; the binary64 instantiation pulls `Classical_Prop.classic`
# transitively through Flocq's `Rcompare` (the compress decision) -- the SAME
# real-comparison lineage that exempts the other expansion-arithmetic files.
# No Admitteds; all lemmas Qed-closed; no dependence on the deferred
# fast_expansion_sum_nonoverlap_shewchuk.  Removable once the parametric refactor
# reaches the binary64 primitives.
theories-flocq/B64_nonoverlap_head.v

# Category C: Shewchuk Theorem 13 §4.A vm_compute cascade traces.
# B64_pathB_trace_4A.v exercises b64_TwoSum / cascade_step_state on concrete
# binary64 witnesses; closure carries Classical_Prop.classic through the same
# B64_Pff_bridge / Rcompare lineage as B64_nonoverlap_head.v above.  No
# Admitteds; all lemmas Qed-closed; refutes pathB_fires_only_when_output_compressed_empty.
theories-flocq/B64_pathB_trace_4A.v

# Category C: C1 division-rounding core (division-by-power-of-two is exact).
# `b64_div_pow2_exact` and its helpers state results about `b64_div` (Flocq's
# `Bdiv`), `Binary.B2R` and `Binary.is_finite`, whose definition closure
# carries `Classical_Prop.classic` -- the SAME b64-arithmetic lineage that
# exempts Orient_b64_exact.v / B64_bridge.v above (those reference
# Bplus/Bminus/Bmult).  No Admitteds in this file; the three lemmas
# (div_bpow_eq_mult_bpow_neg, generic_format_IZR_mult_bpow_neg,
# b64_div_pow2_exact) are Qed-closed.  Removable once the parametric-
# architecture refactor reaches the binary64 arithmetic primitives.
theories-flocq/DivRoundPow2_b64.v

# Category C: Stage D Dekker integer-exactness bridge.  Both lemmas
# (b64_Dekker_exact_of_format, b64_Dekker_int_exact) are stated over
# `b64_Dekker` / `b64_mult` (Flocq's `Bmult` + Pff's Dekker TwoProduct) and
# `Binary.B2R`, whose closure carries `Classical_Prop.classic` -- the SAME
# b64-arithmetic / Pff lineage that exempts B64_Pff_bridge.v (where
# `b64_Dekker_correct` lives) and Orient_b64_exact.v above.  No Admitteds in
# this file; both lemmas are Qed-closed.  Removable once the parametric-
# architecture refactor reaches the binary64 arithmetic primitives.
theories-flocq/Orient_b64_dekker_int.v

# Category C: Stage D orient2d integer-regime nonoverlap + sign-correctness.
# b64_Dekker_finite / b64_orient2d_expansion_int_nonoverlap /
# b64_orient2d_expansion_int_sign_correct are stated over `b64_Dekker` /
# `b64_mult` / `b64_plus` / `b64_minus` (Flocq's Bmult/Bplus/Bminus + Pff
# Dekker) and `Binary.B2R`, whose closure carries `Classical_Prop.classic` --
# the SAME b64-arithmetic / Pff lineage that exempts Orient_b64_dekker_int.v,
# B64_Pff_bridge.v and Orient_b64_expansion.v above.  NOTE: these results do
# NOT depend on the deferred general fast_expansion_sum_nonoverlap_shewchuk
# (verified absent from Print Assumptions) -- they route through the proven,
# unconditional fast_expansion_sum_nonoverlap_shewchuk_int_safe_two_pairs.
# No Admitteds in this file; all three lemmas are Qed-closed.  Removable once
# the parametric-architecture refactor reaches the binary64 primitives.
theories-flocq/Orient_b64_int_safe.v

# Category C: Stage D orient2d integer-COORDINATE guarantee.  The wrapper
# theorems (b64_orient2d_expansion_int_nonoverlap_coords /
# b64_orient2d_expansion_int_sign_correct_coords) and helpers are stated over
# `b64_minus` / the expansion / `cross_R_BP`, whose closure carries
# `Classical_Prop.classic` -- the SAME b64-arithmetic / Pff lineage that exempts
# Orient_b64_int_safe.v, Orient_b64_dekker_int.v and Orient_b64_expansion.v
# above.  As with Orient_b64_int_safe.v, these do NOT depend on the deferred
# general fast_expansion_sum_nonoverlap_shewchuk.  No Admitteds; all theorems
# Qed-closed.  Removable once the parametric refactor reaches the b64 primitives.
theories-flocq/Orient_b64_int_safe_coords.v

# Category C: Stage D orient2d on the Besicovitch-Kakeya gnarlies (concrete demo).
# b64Z / kakeya_inputs_int_safe / kakeya_cross_zero / kakeya_orient2d_sign_zero
# are stated over `binary_normalize` / `cross_R_BP` / the expansion predicate and
# `Binary.B2R`, whose closure carries `Classical_Prop.classic` -- the SAME
# b64-arithmetic lineage that exempts Orient_b64_int_safe_coords.v and
# Orient_b64_exact.v above.  No Admitteds; all three Qed-closed; no dependence on
# the deferred fast_expansion_sum_nonoverlap_shewchuk.  Removable once the
# parametric refactor reaches the binary64 primitives.
theories-flocq/KakeyaOrient2d_b64.v

# Category C: C1 scaled snap bridge.  `b64_snap_coord_scaled_B2R` states a
# result about `b64_div` / `b64_mult` / `b64_snap_coord` (Bnearbyint) and
# `snap_round_coord`, whose closure carries `Classical_Prop.classic` -- the
# same snap-layer / b64-arithmetic lineage that exempts SnapRounding_b64.v
# and HotPixel_b64.v above.  No Admitteds; the bridge is Qed-closed.
theories-flocq/SnapRoundingScale_b64.v

# Category C: Phase 2 milestone 3 (Slice 12) snap-rounding correctness.
# Every theorem here references `snap_round` / `b64_snap` /
# `b64_passes_through_hot_pixel`, defined in HotPixel_b64.v.  Their statements
# pull `Classical_Prop.classic` transitively via Flocq's `round` with
# `round_mode mode_NE` (the real-comparison closure `Rcompare`/`total_order_T`
# behind round-to-nearest-even) and the Liang-Barsky `Rle_bool`/`Req_dec_T`
# machinery -- the SAME lineage that exempts HotPixel_b64.v, where these very
# predicates are defined.  This is intrinsic to any theorem about `snap_round`
# (even the pure-R `snap_round_coord_idem` surfaces `classic`); it is not
# removable without a parametric refactor of the snap layer.  No Admitteds in
# this file; the three deliverable theorems are Qed-closed.
theories-flocq/SnapRounding_b64.v

# Category C: Phase 2 milestone 4 (Slice 13) topological correctness.
# Every theorem references `snap_round` / `b64_snap` (via SnapRounding_b64),
# whose closure pulls `Classical_Prop.classic` through Flocq's `round` /
# `round_mode` -- the identical lineage that exempts SnapRounding_b64.v and
# HotPixel_b64.v.  Intrinsic to any theorem about `snap_round`; not removable
# without a parametric refactor of the snap layer.  No Admitteds in this file;
# the three deliverable theorems are Qed-closed.
theories-flocq/TopologicalCorrectness_b64.v

# Category C: Phase 2 Hobby Theorem 4.1 infrastructure.
# Definitions + conditional theorem + two Admitted supporting lemmas
# (hobby_lemma_4_2, hobby_lemma_4_3 registered in
# docs/admitted-deferred-proofs.txt).  References `snap_round` via
# `snap_round_segments`, pulling `Classical_Prop.classic` through Flocq's
# `round` / `round_mode` -- identical lineage to the other snap files.
theories-flocq/HobbyTheorem_b64.v

# Category C: refutation of hobby_lemma_4_3_no_proper.  An explicit
# parallel-collapse witness whose snap-rounded images intersect properly
# though the originals do not (Qed-closed counterexample; counterexample
# registry).  References `snap_round` (via snap_round_coord), pulling
# `Classical_Prop.classic` through Flocq's `round` / `round_mode` --
# identical lineage to HobbyTheorem_b64.v above.  No Admitteds.
theories-flocq/HobbyCounterexample_b64.v

# Category C: the weaker-but-true noding-preservation claim (separated pairs)
# and its buffer bridgehead.  References `snap_round` / `snap_round_coord`
# (via the tolerance bound and snap_round_segments), pulling
# `Classical_Prop.classic` through Flocq's `round` / `round_mode` -- identical
# lineage to HobbyTheorem_b64.v.  No Admitteds; all Qed-closed.
theories-flocq/NodingSeparation_b64.v

# Category C: Phase 2 follow-up -- b64_passes_through_hot_pixel_halfopen.
# Strict half-open filter (tight characterisation of in_hot_pixel's `<= , <`
# slab convention) used as the b64 mirror of the oracle's PASSES_THROUGH_HALFOPEN
# mode.  References snap_round / b64_snap directly (the snap layer's
# Classical_Prop.classic via Flocq's `round` / `round_mode`).  Same lineage
# as the other snap files.  No Admitteds; sound/complete/bracket all
# Qed-closed.
theories-flocq/PassesThroughHalfopen_b64.v

# Category C: machine-checked soundness counterexample for the rounded compute
# filter.  The theorem statement references `b64_liang_barsky_touches` /
# `b64_passes_through_hot_pixel` (the R-spec, defined in HotPixel_b64.v),
# pulling `Classical_Prop.classic` through the same Liang-Barsky
# `Rle_bool`/`Req_dec_T` + Flocq `B2R` lineage that exempts HotPixel_b64.v.
# Not removable without a parametric refactor of the spec layer.  No
# Admitteds; the counterexample theorem is Qed-closed.
theories-flocq/PassesThrough_b64_compute_unsound.v

# Category C: compute-level half-open => closed refinement.  Pure boolean
# structural lemmas, but their statements reference the compute predicates
# (`b64_liang_barsky_touches_compute` etc.), whose definition closure threads
# `b64_div` / `Binary.Bdiv` and so pulls `Classical_Prop.classic` -- the same
# Flocq-float lineage that exempts the other b64 files.  No Admitteds; the
# refinement theorem is Qed-closed.
theories-flocq/PassesThrough_b64_compute_refine.v

# Category C: machine-checked half-open soundness counterexample.  Same
# lineage as PassesThrough_b64_compute_unsound.v -- references the half-open
# R-spec `b64_liang_barsky_touches_halfopen` (Liang-Barsky `Rle_bool` + Flocq
# `B2R`), so pulls `Classical_Prop.classic`.  No Admitteds; Qed-closed.
theories-flocq/PassesThroughHalfopen_b64_compute_unsound.v

# Category C: machine-checked half-open INCOMPLETENESS counterexample (rounded
# half-open filter drops a real pass).  Same lineage -- references the
# half-open R-spec `b64_passes_through_hot_pixel_halfopen` (Liang-Barsky
# `Rle_bool`/`Rlt_bool` + Flocq `B2R`/`round`), so pulls `Classical_Prop.classic`.
# No Admitteds; Qed-closed.
theories-flocq/PassesThroughHalfopen_b64_compute_incomplete.v

# Category C: machine-checked ASYMMETRY counterexample (rounded passes-through
# filter is not invariant under segment reversal -- the order-dependent-noding
# root behind JTS#752 / JTS#1133).  Both halves are vm_compute, but the file
# imports HotPixel_b64 / PassesThrough_b64_compute, whose binary64 layer pulls
# `Classical_Prop.classic` transitively.  No Admitteds; Qed-closed.
theories-flocq/PassesThrough_b64_compute_asymmetric.v

# Category C: GREEN companion -- the EXACT R-spec passes-through filter IS
# symmetric under segment reversal (b64_passes_through_hot_pixel_symmetric).
# Pure R-arithmetic over the Liang-Barsky bounds; imports HotPixel_b64, whose
# R-spec layer pulls `Classical_Prop.classic` transitively.  No Admitteds.
theories-flocq/PassesThrough_b64_spec_symmetric.v

# Category C (GREEN reduction, same lineage): issue #66 C1 grid-exactness
# REDUCTION layer (docs/snap-rounding-rgr-pivot.md).  On the unit grid the
# snap-consistency conjunct of passes-through is vacuous, reducing compute=spec
# to a single Liang-Barsky touch.  Imports HotPixel_b64 / SnapRounding_b64 /
# PassesThrough_b64_compute, whose binary64 layer pulls `Classical_Prop.classic`
# transitively.  No Admitteds; Qed-closed.
theories-flocq/PassesThrough_b64_grid_exact.v

# Category C (R-side, distinct lineage): issue #64 Option-A atan2 foundation.
# Unlike every other entry here (which pull `classic` through Flocq float ops),
# this is a pure-Stdlib theories/ file: `classic` enters via Stdlib's `Ratan`
# development (the `atan` function itself -- verified: `atan_0` pulls classic
# while `cos_0`/`sqrt_1` do not).  Any atan2-based angle/arc-length proof
# inherits it; this is the intrinsic axiom cost of the JTS-faithful atan2
# representation chosen for #64 (the implicit sin/cos representation would have
# stayed at 3 axioms).  No Admitteds; cos_atan2 / sin_atan2 Qed-closed.
theories/Atan2.v

# Category C (R-side, same atan lineage as Atan2.v): issue #64 Option-A
# foundation 2.  angle_between = atan2 (cross) (dot); imports Atan2 / `atan`,
# so inherits Classical_Prop.classic.  Headlines cos/sin_angle_between and
# atan2_range Qed-closed; no Admitteds.
theories/AngleBetween.v

# Category C (R-side, same atan lineage as AngleBetween.v): buffer/noder
# pipeline seam S3 (docs/buffer-noder-pipeline.md), round-join corner-arc
# relation.  corner_arc_sweep_eq_turn / _unit and atan2_pos_scale are built
# on angle_between = atan2(...), so inherit Classical_Prop.classic.  All
# headlines Qed-closed; no Admitteds.
theories/BufferJoin.v

# Category C (R-side): issue #64 Option-A foundation 3, exact arc length.
# chord_subtended_sq is 3-axiom, but chord_le_arc_length uses Stdlib `sin_lt_x`
# (whose proof pulls Classical_Prop.classic), so the file is 4-axiom.  No
# Admitteds; both headlines Qed-closed.
theories/ArcLength.v

# Category C: one explicitly-documented theorem whose statement cannot be
# parametrised without losing content.  See the `(* CATEGORY C *)` block
# in the file itself; resolution pending policy decision.
theories-flocq/Validate_binary64.v

# Category C: Stage D chain composition.  This file pulls
# `Classical_Prop.classic` transitively via `b64_TwoSum_correct`'s
# closure (same lineage as the other expansion-arithmetic files
# listed above).
#
# The file also contains THREE Admitted theorems with verified
# counterexamples, each registered in `docs/admitted-counterexamples.txt`
# and enforced by `scripts/check_admitted.sh`.  These are
# known-unprovable-as-stated:
#   - `b64_grow_expansion_nonoverlap` (loose general case).
#   - `round_eq_under_strict_dominance` (loose round-eq helper).
#   - `b64_grow_expansion_nonoverlap_dominated` (loose dominated variant).
# The achievable versions (`b64_grow_expansion_nonoverlap_pathA`,
# `round_eq_pathA_{positive, negative, zero}`, etc.) are all Qed-closed
# in this same file.
#
# See `docs/stage-d-grow-expansion-nonoverlap-tangent.md` for the full
# design history and the verified counterexample arithmetic.
theories-flocq/B64_FastExpansionSum.v

# Category C: Slice A pieces 1+2.  This file defines `nonoverlap_shewchuk`
# (weakened predicate tolerating internal zeros) and proves
# `sign_of_expansion_correct_shewchuk` (Qed-closed).  Pulls
# `Classical_Prop.classic` transitively via `sign_of_expansion_correct`'s
# closure on `Binary.B2R` -- same lineage as the other expansion-arithmetic
# files listed above.  No Admitteds in this file.  Foundation for Slice A
# pieces 3-7 (fast-expansion-sum + orient2d_exact composition).
theories-flocq/B64_Expansion_Shewchuk.v

# Category C: Slice A piece 5b Route 2 framework (Session 1).  Defines
# provenance tagging on the cascade input plus the cascade_invariant
# predicate.  Pulls `Classical_Prop.classic` transitively via the
# B2R chain (same lineage as the other expansion-arithmetic files).
# No Admitteds in this file; the load-bearing
# cascade_step_preserves_invariant is deferred to a follow-up session
# (still covered by the existing deferred-proof entry for
# fast_expansion_sum_nonoverlap_shewchuk).
theories-flocq/B64_FastExpansionSum_Shewchuk_Route2.v

# Category C: verified finding that the Route-2 `cascade_pathA_chain`
# reduction is too strong (false on a cross-source opposite-sign leading
# pair).  Single Qed lemma `cascade_handover_fails_mixed_sign`; pulls
# `Classical_Prop.classic` transitively via the same B2R/expansion lineage.
# No Admitteds.  Redirects the deferred fast_expansion_sum_nonoverlap_shewchuk
# proof toward a pathA-OR-pathB invariant.
theories-flocq/B64_Shewchuk_Thm13_pathA_defect.v

# Category C: Slice A piece 3.  Defines `fast_expansion_sum`
# (Shewchuk's canonical algorithm: insertion sort by magnitude
# ascending + TwoSum cascade) and the sort-level sum-preservation
# helpers `expansion_R_insert_by_abs` and `expansion_R_sort_by_abs`
# (both Qed-closed).  Pulls `Classical_Prop.classic` transitively via
# the reused `b64_grow_expansion_aux` from B64_FastExpansionSum.v --
# same lineage as the other expansion-arithmetic files.  No Admitteds
# in this file; pieces 4 and 5 (sum-correctness and
# nonoverlap_shewchuk preservation) are next.
theories-flocq/B64_FastExpansionSum_Shewchuk.v

# Category C: Slice A piece 6.  Composes b64_Dekker + fast_expansion_sum
# into b64_orient2d_expansion.  Pulls `Classical_Prop.classic`
# transitively via the B2R chain (same lineage as the expansion-arithmetic
# files above).  Sum-correctness is Qed-closed (under an explicit
# diffs_exact precondition).  Nonoverlap and sign-correctness theorems
# reference `fast_expansion_sum_nonoverlap_shewchuk` (currently Admitted
# in B64_FastExpansionSum_Shewchuk.v with deferred-proof registry entry).
# That Admitted reference shows up in the file's Print Assumptions
# blocks as an extra axiom; this exception entry covers it.  Per the
# deferred-proof discipline, once Piece 5b closes, the reference becomes
# Qed-closed and only the Category C lineage remains.
theories-flocq/Orient_b64_expansion.v

# Category C: Slice A piece 7.  Stage D headline.  Composes Piece 6's
# expansion-based decoder with the existing Stage A filter into the
# full b64_orient_sign_stage_d decoder + soundness theorem.  Same
# audit lineage as Piece 6: Classical_Prop.classic transitively via
# the B2R chain + fast_expansion_sum_nonoverlap_shewchuk reference
# (deferred-proof, removable when Piece 5b closes).  The tiny-regime
# decisive theorem is Qed-closed without the deferred reference.
theories-flocq/Orient_b64_stage_d.v

# Category C: Issue #66 grounded headline.  Composes Stage D's
# b64_orient_sign_stage_d_sound with the full-plane exact
# integer-determinant model (Orient_b64_exact_full.b64_orient2d_exact_sound)
# to prove the Shewchuk decoder AGREES with the exact model
# (shewchuk_stage_d_agrees_with_exact_intdet) and is decisive in the tiny
# regime (shewchuk_vs_z2_tiny_headline).  No Admitteds in this file; it
# inherits exactly the Stage D lineage: Classical_Prop.classic transitively
# via the B2R chain + the fast_expansion_sum_nonoverlap_shewchuk reference
# (deferred-proof, removable when Piece 5b closes).  Replaces the ungrounded
# "Shewchuk_vs_Z2" Hypothesis draft from the #66 discussion.
theories-flocq/Shewchuk_vs_Z2.v

# Category C: Issue #66 unsoundness witness.  Concrete finite binary64
# points where the float product underflows to +0, so the Stage A filter
# b64_orient_sign_filtered reports OrientRZero while the exact
# integer-determinant model returns +1 and the true cross product is > 0
# (stage_a_filter_unsound_under_underflow, Qed-closed via vm_compute).
# Pulls Classical_Prop.classic transitively via the cross_R_BP / B2R real
# lineage (same as the other binary64-arithmetic files).  No Admitteds,
# and -- unlike the Stage D files -- no fast_expansion_sum_nonoverlap_shewchuk
# reference (it touches only the filter and the exact model).
theories-flocq/Orient_b64_underflow_unsound.v

# Category C: Issue #66 underflow recovery.  Proves the float Stage D
# decoder does NOT recover on the underflow witness (returns OrientRZero),
# defines the integer-mantissa decoder b64_orient_sign_intexact and proves
# it sound over the whole finite plane + correct (OrientRPos) on the
# witness.  Pulls Classical_Prop.classic transitively via the cross_R_BP /
# B2R real lineage (same as the other binary64 files).  No Admitteds, and no
# fast_expansion_sum_nonoverlap_shewchuk reference (Stage D's value is only
# vm_computed; its deferred-proof soundness theorem is not used).
theories-flocq/Orient_b64_underflow_recovery.v

# Category C: Issue #66 corrected Stage D (b64_orient_sign_stage_d_safe).
# Routes every non-committal filter verdict through the integer-mantissa
# exact decoder; sound whenever the filter is indefinite (no precondition),
# sound under int-safety alone (drops expansion-safety), total, and correct
# on the underflow witness.  Pulls Classical_Prop.classic transitively via
# the cross_R_BP / B2R real lineage (same as the other binary64 files).  No
# Admitteds, and no fast_expansion_sum_nonoverlap_shewchuk reference (the
# corrected decoder abandons the expansion path -- the exact fallback is the
# integer-mantissa model, not the EFT expansion).
theories-flocq/Orient_b64_stage_d_safe.v

# Category C: Phase 3 Milestone 4 (Flocq layer) -- noding-to-graph bridge.
# Defines noded_segments and proves snap_noding_bridge +
# valid_topology_graph_noded_labeled_graph by lifting the R-side
# `valid_topology_graph_build_graph` / `valid_topology_graph_build_labeled_graph`
# through Phase 2's snap_round_segments.  Inherits Classical_Prop.classic
# transitively from HobbyTheorem_b64's snap_round_segments (Flocq binary
# arithmetic closure).  No Admitteds added by this file.
theories-flocq/OverlayBridge.v

# Category C: Phase 3 Milestone 5 Session 15 -- the conditional headline.
# Defines and proves overlay_ng_correct_conditional (Qed-closed) plus
# its forward/backward corollaries.  Imports OverlayBridge for
# correct_labels_all_ops + valid_topology_graph_noded_labeled_graph,
# which transitively pull Classical_Prop.classic via snap_round_segments
# (same lineage as OverlayBridge above).  Three named hypotheses
# (JCT, DCEL valid polygons, semantic bridge) carried in the theorem's
# signature.  Post-S15 update: the opaque Section Variable
# `geometric_interior` was replaced with the concrete
# `geometric_interior_stdlib` from `theories/PointInRingTangents.v`
# -- the topological-interior definition (pure Stdlib R, no fourcolor),
# strengthening the H1 hypothesis from "any predicate" to "the concrete
# bounded-component-of-complement predicate".  The JCT gap remains
# in the H1 biconditional's content, not in the predicate definition.
# No Admitteds added by this file.
theories-flocq/OverlayCorrectness.v
