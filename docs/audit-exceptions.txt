# Audit exceptions for `scripts/audit_axioms.sh`
#
# Each non-comment, non-blank line names a file (relative to repo root)
# whose `Print Assumptions` blocks are exempted from the allowlist check.
# Listed files may pull axioms beyond `docs/axiom-allowlist.txt`; their
# contamination is the *known, transitional* state captured below.
#
# The discipline this file enforces:
#   - Files NOT listed here MUST satisfy the allowlist on every CI run.
#   - Files listed here are tracked for cleanup; removing a file from this
#     list is the final step of refactoring it to be classic-clean.
#   - Adding a file to this list requires PR justification (a comment
#     immediately above the file line explaining what makes the file
#     Category C / transitional and the planned resolution).
#
# Current state (2026-05-16): 11 files in `theories-flocq/` pull
# `Classical_Prop.classic` per theorem because their theorem statements
# substantively reference `Binary.Bplus` / `Bminus` / `Bmult`, which
# carry `classic` in their definition closure.  This contamination is a
# pre-existing condition the corpus inherited; the parametric-architecture
# refactor (started in commit b010037 on `Validate_binary64.v`) is
# clearing files one by one.  Removal from this list is the per-file
# completion criterion.

# Transitional: clean of `classic` once parametric-architecture refactor
# applied per the policy designed in `docs/category-c-policy.md`
# (to be written).
theories-flocq/Orient_b64_exact.v
theories-flocq/Intersect_b64.v
theories-flocq/B64_bridge.v
theories-flocq/Intersect_b64_exact.v
theories-flocq/B64_lib.v
theories-flocq/Orient_b64_R.v
theories-flocq/Validate_binary64_bridge.v
theories-flocq/Orientation_b64.v
theories-flocq/HotPixel_b64.v
theories-flocq/B64_Pff_bridge.v
theories-flocq/Orient_b64_sound.v

# Category C: one explicitly-documented theorem whose statement cannot be
# parametrised without losing content.  See the `(* CATEGORY C *)` block
# in the file itself; resolution pending policy decision.
theories-flocq/Validate_binary64.v

# Stage D chain composition.  `b64_grow_expansion_correct` (sum
# preservation) is Qed-closed; `b64_grow_expansion_nonoverlap` (the
# load-bearing nonoverlap theorem) is still Admitted, deferred to the
# follow-up session.  The Print Assumptions block for the closed
# theorem pulls `Classical_Prop.classic` transitively via
# `b64_TwoSum_correct`'s closure (same Category C contamination as the
# other expansion-arithmetic files); the file-level exemption covers
# both that PA leak and the remaining Admitted's audit footprint.
# Comes off this list when both theorems are Qed-closed.  Also flagged
# by CI's Qed-invariant grep for the remaining Admitted -- that flag
# is the correct behaviour, see the design doc and commit messages.
theories-flocq/B64_FastExpansionSum.v
